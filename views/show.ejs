<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title><%= filename %> - Task Manager</title>
</head>
<body class="bg-gradient-to-br from-zinc-900 via-zinc-800 to-zinc-900 text-white min-h-screen">
    
    <div class="main w-full min-h-screen flex flex-col items-center justify-start py-10 px-4">
        
        <!-- Back Link -->
        <div class="w-full max-w-4xl mb-6">
            <a href="/" class="inline-flex items-center px-4 py-2 bg-zinc-700 hover:bg-zinc-600 rounded-lg text-blue-400 hover:text-blue-300 font-medium transition-all duration-200">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
                Go back to home
            </a>
        </div>
        
        <!-- Task Content -->
        <div class="w-full max-w-4xl bg-zinc-800/50 backdrop-blur-sm rounded-xl shadow-2xl p-8 border border-zinc-700">
            <h1 class="text-4xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-500 mb-4 tracking-tight"><%= filename %></h1>
            <p class="text-lg text-zinc-300 leading-relaxed"><%= filedata %></p>
        </div>
    </div>
</body>
</html>
